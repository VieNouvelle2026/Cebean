Drop your three product demo clips in this folder with these exact filenames:

- exportready-demo.mp4
- contractor-audit-shield-demo.mp4
- invorapro-demo.mp4

Recording tips:
- 1280x720 or higher, cropped to the relevant UI (not the full browser/desktop)
- 8-15 seconds, ideally looping cleanly (similar start/end state)
- MP4 (H.264), muted — no audio track needed, they autoplay muted on loop
- Compress to roughly 2-4MB each so pages stay fast

Until a file is added, the site shows a clean placeholder instead of a broken video icon —
no code changes needed once you drop the real files in.
